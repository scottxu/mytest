<%= form_for(@mya) do |f| %>
  <% if @mya.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mya.errors.count, "error") %> prohibited this mya from being saved:</h2>

      <ul>
      <% @mya.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
