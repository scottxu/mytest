<%= form_for(@myb) do |f| %>
  <% if @myb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@myb.errors.count, "error") %> prohibited this myb from being saved:</h2>

      <ul>
      <% @myb.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mya_id %><br>
    <%= f.number_field :mya_id %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
